'use client';
import { PlaceholderApp } from '@/modules/PlaceholderApp';

export default function Page() {
  return (
    <PlaceholderApp
      active="config"
      title="Configuración"
      subtitle="Ajustes de la cuenta, usuarios y preferencias del equipo."
      art="document"
    />
  );
}
